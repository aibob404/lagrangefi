name: Build and Push Images

on:
  push:
    branches: [main]
  workflow_dispatch: # manual build for feature branches

env:
  OWNER: aibob404

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      chain: ${{ steps.result.outputs.chain }}
      api: ${{ steps.result.outputs.api }}
      web: ${{ steps.result.outputs.web }}
    steps:
      - uses: actions/checkout@v4

      # Path filtering only makes sense on push (has a before/after diff).
      # Manual builds always build all services.
      - uses: dorny/paths-filter@v3
        id: filter
        if: github.event_name == 'push'
        with:
          filters: |
            chain:
              - 'apps/chain/**'
              - 'packages/shared/**'
            api:
              - 'apps/api/**'
            web:
              - 'apps/web/**'

      - name: Set outputs
        id: result
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "chain=true" >> $GITHUB_OUTPUT
            echo "api=true"   >> $GITHUB_OUTPUT
            echo "web=true"   >> $GITHUB_OUTPUT
          else
            echo "chain=${{ steps.filter.outputs.chain }}" >> $GITHUB_OUTPUT
            echo "api=${{ steps.filter.outputs.api }}"     >> $GITHUB_OUTPUT
            echo "web=${{ steps.filter.outputs.web }}"     >> $GITHUB_OUTPUT
          fi

  build-chain:
    needs: changes
    if: needs.changes.outputs.chain == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set short SHA
        id: sha
        run: echo "short=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push chain
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/chain/Dockerfile
          push: true
          tags: ghcr.io/${{ env.OWNER }}/lagrangefi-chain:sha-${{ steps.sha.outputs.short }}
          cache-from: type=gha,scope=chain
          cache-to: type=gha,mode=max,scope=chain

  build-api:
    needs: changes
    if: needs.changes.outputs.api == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set short SHA
        id: sha
        run: echo "short=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push api
        uses: docker/build-push-action@v5
        with:
          context: apps/api
          push: true
          tags: ghcr.io/${{ env.OWNER }}/lagrangefi-api:sha-${{ steps.sha.outputs.short }}
          cache-from: type=gha,scope=api
          cache-to: type=gha,mode=max,scope=api

  build-web:
    needs: changes
    if: needs.changes.outputs.web == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set short SHA
        id: sha
        run: echo "short=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push web
        uses: docker/build-push-action@v5
        with:
          context: apps/web
          push: true
          tags: ghcr.io/${{ env.OWNER }}/lagrangefi-web:sha-${{ steps.sha.outputs.short }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web

  update-manifests:
    needs: [build-chain, build-api, build-web]
    # Only update kustomization.yaml on push to main, not on manual builds
    if: |
      always() &&
      github.event_name == 'push' &&
      (needs.build-chain.result == 'success' || needs.build-api.result == 'success' || needs.build-web.result == 'success')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set short SHA
        id: sha
        run: echo "short=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      # Only update tags for services that were actually rebuilt
      - name: Update image tags in test overlay
        run: |
          TAG="sha-${{ steps.sha.outputs.short }}"
          if [ "${{ needs.build-chain.result }}" = "success" ]; then
            yq e '(.images[] | select(.name == "lagrangefi/chain") | .newTag) = "'"$TAG"'"' -i k8s/overlays/test/kustomization.yaml
          fi
          if [ "${{ needs.build-api.result }}" = "success" ]; then
            yq e '(.images[] | select(.name == "lagrangefi/api") | .newTag) = "'"$TAG"'"' -i k8s/overlays/test/kustomization.yaml
          fi
          if [ "${{ needs.build-web.result }}" = "success" ]; then
            yq e '(.images[] | select(.name == "lagrangefi/web") | .newTag) = "'"$TAG"'"' -i k8s/overlays/test/kustomization.yaml
          fi

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/overlays/test/kustomization.yaml
          git diff --staged --quiet || git commit -m "chore: update test images to sha-${{ steps.sha.outputs.short }} [skip ci]"
          git push
