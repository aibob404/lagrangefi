# Copy this file to .env.development.local and fill in your values.
# .env.development.local is gitignored — never commit it.

# Enable mock API (no real server needed)
VITE_USE_MOCK=true

# Or set the real API server to proxy to (when MOCK=false)
# API_HOST=http://<server-ip>
